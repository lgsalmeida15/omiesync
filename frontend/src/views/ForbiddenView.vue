<template>
  <div class="p-6 text-center" style="padding-top:80px">
    <p style="font-family:var(--font-display);font-size: var(--fs-xs);color:var(--text-dim);letter-spacing:2px">ERRO 403</p>
    <p style="font-size: var(--fs-lg);font-weight:800;margin-top:8px;color:var(--text)">Acesso negado</p>
    <p style="font-size: var(--fs-sm);color:var(--text-muted);margin-top:8px">Você não tem permissão para acessar esta página.</p>
    <RouterLink to="/" style="color:var(--primary);font-size: var(--fs-xs);font-family:var(--font-display);display:inline-block;margin-top:20px">
      ← Voltar ao início
    </RouterLink>
  </div>
</template>
